<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- POST_NOTIFICATIONS required on Android 13 (API 33+) for push notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- WRITE_EXTERNAL_STORAGE only needed on Android 9 and below -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaestroAutoworks">

        <!-- 0. Animated Splash Screen (LAUNCHER) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MaestroAutoworks.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 1. Public Landing Page -->
        <activity android:name=".activities.MainActivity" android:exported="false"/>

        <!-- 2. Login -->
        <activity android:name=".activities.LoginActivity" android:exported="false"/>

        <!-- 3. Register -->
        <activity android:name=".activities.RegisterActivity" android:exported="false"/>

        <activity android:name=".activities.ForgotPasswordActivity" android:exported="false"/>
        <!-- 4. Customer Home -->
        <activity android:name=".activities.HomeActivity" android:exported="false"/>

        <!-- 5. Services -->
        <activity android:name=".activities.ServicesActivity" android:exported="false"/>

        <!-- 6. Book Appointment -->
        <activity android:name=".activities.BookActivity" android:exported="false"/>

        <!-- 7. My Appointments -->
        <activity android:name=".activities.AppointmentsActivity" android:exported="false"/>

        <!-- 8. About -->
        <activity android:name=".activities.AboutActivity" android:exported="false"/>

        <!-- 9. Identity Verification (OTP / 2FA) -->
        <activity android:name=".activities.IdentityVerificationActivity" android:exported="false"/>

        <!-- 11. Search -->
        <activity android:name=".activities.SearchActivity" android:exported="false"/>

        <!-- ADMIN SECTION -->
        <!-- A1. Admin Dashboard -->
        <activity android:name=".activities.AdminDashboardActivity" android:exported="false"/>
        <!-- A2. Admin Appointments (standalone) -->
        <activity android:name=".activities.AdminAppointmentsActivity" android:exported="false"/>
        <!-- A3. Admin Services (standalone) -->
        <activity android:name=".activities.AdminServicesActivity" android:exported="false"/>
        <!-- A4. Admin Reports (standalone) -->
        <activity android:name=".activities.AdminReportsActivity" android:exported="false"/>
        <!-- A5. Admin PMS / Repair Tracker (standalone) -->
        <activity android:name=".activities.AdminPmsActivity" android:exported="false"/>


        <!-- FileProvider for OR/CR camera capture on Android 9 and below -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths"/>
        </provider>

    </application>
</manifest>